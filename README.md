# ⚡ XoomScript v0.2.1

**XoomScript** — это низкоуровневый компилируемый язык программирования, использующий мощь **LLVM**. Он создан для тех, кто хочет контролировать каждый регистр, не теряя при этом удобства современных языков.



## ✨ Ключевые особенности
* 🚀 **JIT Компиляция:** Мгновенное выполнение кода через LLVM MCJIT.
* 📦 **Модульность:** Система `#include <lib_name>` для организации кода.
* 🧠 **Гибридная память:** Работайте с регистрами (`rax`, `rbx`) или глобальными переменными (`closall var`).
* 📋 **Mixed Lists:** Уникальная поддержка списков со смешанными типами данных (`int`, `str`, `dbl`).
* 🛠 **Native Build:** Возможность компиляции в полноценный `.exe` через Clang.

## 🛠 Установка
1. Установите Python 3.10+
2. Установите зависимости:
pip install llvmlite
3 Установите Clang (если планируете собирать .exe).
🚀 Быстрый старт
Создайте файл main.xs

closall var int 100 name=money
set list inventory = {1, "Sword", 5.5}

func show_status {
    printconsole("Current money: money")
} endfunc

call show_status
check (money > 50) = printconsole("You are rich!") else = printconsole("Go work!")
Запустите код:
python main.py run main.xs

🤝 Контрибьютинг
Если у вас есть идеи по улучшению синтаксиса или оптимизации компилятора — создавайте Issue или Pull Request!

Разработано с любовью к низкоуровневому коду. Автор: zuterog
